This is a work stack. Take the top one and process it.

- (session-management.ts) \* TODO: Rename this to getOpenNamedSessionsFromStorage() as it does not handle closed ones.
- (tabs.ts, session-management.ts, service-worker-interface.ts, service-worker-handler.ts) Improve session restoration on browser start. We can refer the sessionId in URL, but it is not working well yet.
- Find a TODO from codebase and let's work on it.

---

Refer .clinerules as well.
